# ── AI Video Assistant ── requirements.txt ───────────────────────────────────
# Python >= 3.10 recommended (set via runtime.txt for Streamlit Cloud)

# ── Audio / Video Acquisition ─────────────────────────────────────────────────
yt-dlp                            # Download videos/audio from YouTube
pydub                             # Audio manipulation and conversion
ffmpeg-python                     # Python bindings for FFmpeg
# FFmpeg binary must be installed separately (see packages.txt)

# ── Speech-to-Text ────────────────────────────────────────────────────────────
openai-whisper                    # Local OpenAI Whisper speech recognition
torch                             # PyTorch backend for Whisper
torchaudio                        # PyTorch audio utilities

# ── Translation ───────────────────────────────────────────────────────────────
deep-translator                   # Translation wrapper

# ── LLM Orchestration — LangChain + Mistral ───────────────────────────────────
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-mistralai                # LangChain integration for Mistral
mistralai                          # Official Mistral Python client

# ── RAG Pipeline / Vector Database ────────────────────────────────────────────
chromadb                          # Local vector database
langchain-chroma                  # LangChain ↔ Chroma integration
sentence-transformers             # HuggingFace embedding models
langchain-huggingface             # LangChain ↔ HuggingFace integration
huggingface-hub                   # HuggingFace model downloads
tiktoken                          # Tokenization / token counting

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit
streamlit-extras                  # Additional Streamlit components
watchdog                          # Streamlit file watching / hot reload

# ── PDF Generation / Export ───────────────────────────────────────────────────
reportlab                         # PDF generation
fpdf2                             # Lightweight PDF generation

# ── Environment / Utilities ───────────────────────────────────────────────────
python-dotenv                     # Load environment variables from .env
requests                          # HTTP requests
numpy
pandas
tqdm                              # Progress bars

# ── Optional / Image Support ──────────────────────────────────────────────────
torchvision                       # Image/vision utilities when required
pillow                            # Image processing
scikit-learn                      # ML utilities / similarity / preprocessing

audioop-lts
